{% extends 'base.html.twig' %}

{% block title %}Liste{% endblock %}

{% block body %}
  <section class="bars">
    <div class="container">
      <div class="row">
        {% for film in films %}
          <article>
            <h2>{{ film.nom }}</h2>
            <p>{{ film.synopsis }}</p>
            <p>{{ film.type }}</p>
          </article>
        {% endfor %}
      </div>
    </div>
  </section>
  <section>

  </section>
{% endblock %}
